import FBLogin from 'components/FBLogin';

export default ({}) => (
    <div>
        <h2 className="cover-heading">Sign up to see pets available for adoptable.</h2>
        <p className="lead">
            <FBLogin className="btn btn-lg btn-primary" />
        </p>
    </div>
);